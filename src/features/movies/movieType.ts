type movie = {
    _id: string,
    title: string,
    overview: string,
    release_date: string,
    poster: string,
    backdrop: string,
    rating: number,
    seen: boolean,
}

export default movie;
